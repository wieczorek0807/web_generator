import 'package:flutter/material.dart';

class BorderRadiusScreen extends StatelessWidget {
  const BorderRadiusScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Border Radius screen'),
      ),
    );
  }
}
